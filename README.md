# selenium_stepik
Stepik.org automation course project
- Clone the repo `git clone`
- Install requirements `pip install -r requirements.txt`
- Run tests `pytest -v --tb=line --language=en -m need_review`